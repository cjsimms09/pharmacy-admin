# Copy to .env and fill in locally. .env is git-ignored and Claude is denied access to it.
# Every value here is a placeholder. Real values live only in the production secret store.
DATABASE_URL=postgres://user:EXAMPLE@localhost:5432/pharmacy_admin
APP_ENCRYPTION_KEY=EXAMPLE_32_BYTE_BASE64_KEY
RX_TOKEN_HMAC_KEY=EXAMPLE_32_BYTE_BASE64_KEY
INGEST_WEBHOOK_SECRET=EXAMPLE
