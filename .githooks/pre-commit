#!/bin/sh
# Refuses a commit carrying a real identifier. Install once: git config core.hooksPath .githooks
# The rule is src/lib/identifier-scan.ts, and it reads what is STAGED, not the working copy.
npx tsx scripts/check-identifiers.ts || exit 1
